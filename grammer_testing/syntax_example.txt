main: in (i32 a, i32 b) {
    (a, b) = plusplus(1, 2, 3, 4)

    int sum = calculateSum(a, b);

    if (sum > 10) {
        print("Sum: " + sum + " is bigger than 10");
    } else if (sum > 5) 
        print("Sum: " + sum + " is bigger than 5");
    else {
        print("Sum: " + sum + " is less than or equal to 5");
    }

    printHelloWorld();
}

printHelloWorld: {
    print("Hello World");
}

plusplus: in (i32 a, i32 b, i32 c, i32 d) out (z: i32, x: i32) {
    z = a + b;
    x = c + d;
}

calculateSum: in (i32 x, i32 y) out (i32 r) {
    r =+ y;
}